import React, { Component } from 'react';
import TileContainer from '../commonComponents/tileContainer';

const SampleOptions = (props) => {
    return (
        <div>
            <TileContainer/>
        </div>
    )
}

export default SampleOptions;